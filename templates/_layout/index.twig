<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
<title>Starter Site</title>
{#<title>{{ (title ? "#{title} | ") ~ systemName }}</title>#}
{#{% include '_includes/favicon-set' %}#}
{#{% include '_includes/app-variables' %}#}
{% do view.registerAssetBundle(LayoutAssets) %}
</head>

<body>
    {% include '_layout/header' %}
    <div class="bg-orange-100" style="min-height:600px">
        {% block content null %}
    </div>
    {% include '_layout/footer' %}
</body>
</html>
