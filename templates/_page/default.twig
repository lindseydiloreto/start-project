{% extends '_layout' %}
{% set title = entry.title %}

{% block content %}
    <h1 class="font-bold text-3xl">{{ entry.title }}</h1>
    {% include '_panels' with {'matrix': entry.panels} only %}
{% endblock %}
