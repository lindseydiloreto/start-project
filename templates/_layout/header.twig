<header class="text-center font-bold py-6 bg-blue-100">
    <nav>
        <a href="{{ url() }}">Home</a> &nbsp;&bull;&nbsp;
        <a href="{{ url('news') }}">News</a> &nbsp;&bull;&nbsp;
        <a href="{{ url('redactor') }}">Entry</a>
    </nav>
</header>
